# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Локальные данные / БД (лежат на постоянном диске /data, не в репо)
*.db
*.db-wal
*.db-shm
/data/
exports/

# …но файл-сид для переезда базы коммитить МОЖНО (см. _seed_raw_db_if_present)
!seed_nft_cache.db
!seed_nft_cache.db-wal
!seed_nft_cache.db-shm

# Папка с выгруженной со старого сервера базой (700 МБ+) — НЕ коммитить!
амвера/
amvera-data/

# Логи и временное
*.log
.DS_Store

# IDE
.vscode/
.idea/
